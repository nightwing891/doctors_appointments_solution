<h1>Users</h1>

<%= link_to 'New User', new_user_path %>

<h2><%= @users.length <= 0 ? "No Users" : "" %></h2> 

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.full_name, user %>
    </li>
  <% end %>
</ul>